# Handoff architecte — 2026-05-10

Note de transmission de 5 sujets ouverts à reprendre en cycle architecte dédié. **Ces sujets sortent du pilotage de la session Claude Code courante** ; la session ne les suit plus, ne les relance pas, ne les rappelle pas.

Chaque item est self-contained pour démarrer une réflexion sans contexte préalable de la session précédente.

---

## 1. Acter les verdicts A/B/C — analyse `poc-scale-benchmark`

**Statut** : Rapport d'analyse écrit, **verdicts non actés**.

**Artefacts** :
- Rapport : `docs/reports/2026-05-10_analyse-annotations-poc-scale-benchmark.md`
- Source annotations : `docs/reports/poc-scale-benchmark/annotations.json` (222 entrées grille v2)
- Skill arbitre : `.claude/skills/prompt-taxonomy-ecosystem.skill`

**Verdicts auto produits** :
- **A** — Fix `2_objets` partiel (7/13 éteints, 6/13 résiduels — Solo animal dynamique : `bactrian_camel`, `golden_retriever`, `mountain_gorilla`, `playful_dolphin`, `running_cheetah`, `running_giraffe`)
- **B** — Méta-patterns : la mécanique skill = OK (0% sur 4 défauts canoniques), mais **2 tags émergents** (`image_pas_coherente` 66, `image_incomprehensible` 64) signalent un mismatch demande/capacité ERNIE non cartographié dans le skill (cf. insight produit point 4)
- **C** — Classes "OU" : 6/13 cohérentes (et non 12 attendues — délta brief vs réalité). 7 incohérentes à scores 1.00–3.83 : `Comparatif before/after OU Solo`, `Imagier annoté 3×3 OU Solo visage`, `Imagier différencié OU Solo`, `Solo humain OU objet` (bordeline 3.83), `Solo objet ou Humain+entité`, `Solo objet ou comparatif`, `Solo objet ou scène` (bordeline)

**Action attendue** :
1. Lire le rapport d'analyse en entier
2. Trancher chaque verdict (acter / partiellement acter / rejeter et re-itérer)
3. Inscrire la décision dans MEMORY.md "Décisions architecturales actées"
4. Mettre à jour les critères de sortie du cycle en cours (lignes 14–16 de MEMORY.md, déjà cochés ✅ "annotés" mais pas "actés")

**Lien avec point 4** : le verdict B dépend de la décision sur la capitalisation skill (un Tn dédié à l'insight ERNIE peut requalifier `image_pas_coherente`/`image_incomprehensible`).

---

## 2. Cadrage MEP v0

**Statut** : Inscrit comme **bloqueur n°1** dans MEMORY.md (centre de gravité 2026-05-10), périmètre + critères + date à définir.

**Contexte** :
- État courant projet (MEMORY.md ligne 7) : "Centre de gravité 2026-05-10 : préparer la MEP v0 — périmètre, critères et date à cadrer en priorité. Toute qualification automatique des sujets candidats est reportée post-MEP v0 ou MEP+30j (cf. décisions Plans A/B/C 2026-05-10)."
- Plans A/B/C référencés mais non visibles dans la portion non-tronquée de MEMORY.md — à vérifier dans les rapports `docs/reports/2026-05-10_*` ou `docs/architect/briefs/2026-05-10_*` éventuels.

**Action attendue** :
1. Définir périmètre fonctionnel MEP v0 (scope produit minimum publiable)
2. Définir critères de sortie mesurables (qualité, volume, couverture)
3. Fixer date cible
4. Inscrire dans MEMORY.md (table Échéances, ligne 43 actuellement marquée "**à faire — bloqueur n°1**")

**Sans ce cadrage** : Plans B/C (qualification automatique) restent flous et toute priorisation post-validation humaine est suspendue.

---

## 3. Décision ~31 classes confidence non-Haute strict

**Statut** : Deadline 2026-05-14 (table Échéances ligne 31), à faire.

**Contexte** :
- Couverture cumulée actuelle : ~54/85 workflow_classes
- ~31 classes restantes en `confidence: Moyenne` (ou plus bas) jamais testées en scale-bench
- 3 options sur la table : run élargi (`startswith("Haute")`), exclusion explicite, traitement manuel
- Estimation : 30–60 min de génération si run élargi
- Référence : `docs/reports/2026-05-09_poc-scale-uncovered-classes.md`

**Action attendue** :
1. Trancher l'option (run élargi / exclusion / manuel)
2. Si run élargi : briefer un agent pour exécuter
3. Inscrire la décision dans MEMORY.md

**Dépendance** : "Dépend partiellement de la visibilité que donne l'annotation en cours" (= verdict B du point 1).

---

## 4. Capitalisation skill — insight ERNIE variation/répétition

**Statut** : Insight capturé en mémoire utilisateur (`project_ernie_variation_insight.md`), **pas d'action skill**.

**Insight** : ERNIE est fort en variation libre, faible en répétition stricte. Les 4 workflow_classes multi-cellules (`Grille imagier annoté`, `Imagier différencié 3×3`, `Frise narrative 1×N`, `Multi-sujets via grille`) demandent actuellement la répétition stricte avec un changement par étape — c'est ramer contre le modèle. Pivot produit possible : format "jeu des différences" qui exploite la variation native.

**Artefacts** :
- Mémoire user : `C:\Users\HP\.claude\projects\D--projets-artiste-coloriage\memory\project_ernie_variation_insight.md`
- Source observation : 130+ tags `image_pas_coherente`/`image_incomprehensible` sur les 4 classes méta-patterns dans `docs/reports/poc-scale-benchmark/annotations.json`
- Skill cible : `.claude/skills/prompt-taxonomy-ecosystem.skill` (zip)

**Action attendue (3 choix possibles)** :
1. **Pivot produit** — réorienter les 4 workflow_classes vers format "jeu des différences", écrire un brief pour repenser les templates et libérer la consigne ("un changement par étape" → consigne plus large)
2. **Capitaliser sans pivot** — ajouter une entrée Tn dans le skill cadrée comme **insight modèle** (orientation d'usage), pas comme défaut à éviter
3. **Différer** — laisser l'insight en mémoire, attendre un volume d'annotation supérieur sur ces classes avant de trancher

**Lien avec point 1** : conditionne le verdict B (les défauts émergents sont-ils des défauts à corriger, ou le symptôme d'un mismatch à pivoter ?).

---

## 5. Working tree — annotations en cours non commitées

**Statut** : 3 fichiers `M` dans le working tree (annotations utilisateur en cours d'édition continue).

**Fichiers** :
- `docs/reports/poc-batch-size/annotations.json`
- `docs/reports/poc-scale-benchmark/annotations.json`
- `docs/reports/poc-taxonomy-subjects/annotations.json`

**Statut** : Volontairement laissés hors des commits techniques de la session 2026-05-09/10. Ce sont des données d'annotation **vivantes** (l'utilisateur peut continuer à les modifier au fil des revues).

**Action attendue** : décider d'une cadence de commit (ex. snapshot quotidien `chore(data): snapshot annotations 2026-05-XX`), ou laisser libre.

**Pas urgent** — uniquement de l'hygiène git pour préserver l'historique d'annotation.

---

## Note de coordination

- Cette note **n'est pas exhaustive** sur l'état du projet — pour ça, lire `docs/architect/MEMORY.md` qui reste la source de vérité architecte.
- Tout ajout de sujet non listé ici est hors-périmètre de cette transmission.
- Les commits utiles à connaître sur la séquence livraison 2026-05-09/10 :
  - `164c02c` annotateur v2 · `ca7118f` fix score-row · `956810e` clôture archi annotateur v2
  - `b60ff27`–`c3102b3` hygiène repo (gitignore + 6 commits techniques séparant les chantiers)
  - `ee9e778` `8b96c32` `fd9b97d` greffon prod
  - `b89cb8d` `1e4596e` brief F vocabulaires source unique
  - `efb9429` `89e3d08` fix copie prompt Tailscale
