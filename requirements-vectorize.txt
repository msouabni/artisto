# Dependances service vectorisation (src/services/vectorizer.py + scripts/vectorize_cli.py).
#
# Moteur : VTracer (Rust port via PyO3) -- choisi apres bench du 2026-05-30.
# Aucun binaire systeme requis. Voir _lab/vectorize-bench/VERDICT.md.
#
# Le pre-clean OpenCV reste opt-in (--pre-clean cote CLI, params.pre_clean=True
# cote API) -- utile uniquement si le raster source est degrade. VTracer gere
# deja un filter_speckle natif suffisant pour les sorties ERNIE.

vtracer>=0.6.15
opencv-python-headless>=4.9
numpy>=1.24
